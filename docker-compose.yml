services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    environment:
      - POSTGRES_USER=hrmonitor
      - POSTGRES_PASSWORD=hrmonitor
      - POSTGRES_DB=hrmonitor
    volumes:
      - ts-data:/var/lib/postgresql/data
    expose:
      - "5432"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    expose:
      - "6379"
    restart: unless-stopped

  backend:
    build: ./apps/backend
    environment:
      - DATABASE_URL=postgres://hrmonitor:hrmonitor@timescaledb:5432/hrmonitor
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=hrmonitor_backend=info
    expose:
      - "3001"
    depends_on:
      - timescaledb
      - redis
    restart: unless-stopped

  frontend:
    build: ./apps/frontend
    expose:
      - "3000"
    environment:
      - BACKEND_URL=http://backend:3001
    depends_on:
      - backend
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - frontend
    restart: unless-stopped
    profiles:
      - prod

volumes:
  ts-data:
